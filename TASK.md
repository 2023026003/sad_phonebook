# TASK.md — 전화번호 관리 시스템 구현 태스크

> PRD v1.0.0 기준 | 마일스톤 순서대로 진행

---

## 진행 상태 범례
- `[ ]` 미완료
- `[/]` 진행 중
- `[x]` 완료

---

## M1. 프로젝트 환경 설정

### 1-1. Supabase 설정
- [x] Supabase 계정 생성 및 새 프로젝트 생성
- [x] `contacts` 테이블 생성 (SQL Editor에서 실행)
- [x] Supabase 프로젝트 URL 및 anon key 복사해두기
- [ ] (선택) `updated_at` 자동 갱신을 위한 트리거 함수 생성

### 1-2. Next.js 프로젝트 초기화
- [x] `npx create-next-app@latest ./` 로 프로젝트 생성
- [x] `.env.local` 파일 생성 및 환경 변수 설정
- [x] `.gitignore`에 `.env.local` 포함 여부 확인
- [x] `@supabase/supabase-js` 패키지 설치

### 1-3. Supabase 클라이언트 모듈 생성
- [x] `lib/supabaseClient.js` 파일 생성
- [x] Supabase 연결 동작 확인 (콘솔 로그 테스트)

### 1-4. GitHub 저장소 설정
- [ ] GitHub 원격 저장소 생성
- [ ] 로컬 프로젝트와 원격 저장소 연결 (`git remote add origin ...`)
- [ ] 초기 커밋 및 push

---

## M2. 연락처 목록 조회 (Read)

> 관련 요구사항: FR-01, FR-02, FR-03, US-01

- [x] `app/page.js` (또는 `pages/index.js`)에서 Supabase `contacts` 테이블 전체 조회
- [x] 조회 결과를 state에 저장하고 화면에 목록으로 렌더링
- [x] 각 항목에 이름, 전화번호, [수정] 버튼, [삭제] 버튼 표시 (FR-02)
- [x] 연락처가 없을 경우 "등록된 연락처가 없습니다." 문구 표시 (FR-03)
- [x] 페이지 로드 시 자동으로 목록 불러오기

---

## M3. 연락처 등록 (Create)

> 관련 요구사항: FR-04, FR-05, FR-06, FR-07, US-02

- [x] 이름 입력 필드 구현 (FR-04)
- [x] 전화번호 입력 필드 구현 (FR-04)
- [x] [추가] 버튼 구현
- [x] 클라이언트 유효성 검사 구현 (FR-05)
  - [x] 이름 미입력 시 에러 메시지 표시
  - [x] 전화번호 미입력 시 에러 메시지 표시
- [x] 전화번호 형식 검사 — 숫자 + 하이픈만 허용 (FR-06)
  - 정규식 예: `/^[0-9-]+$/`
- [x] Supabase `insert` 호출로 DB에 연락처 저장
- [x] 등록 성공 시 입력 필드 초기화 (FR-07)
- [x] 등록 성공 시 목록 즉시 갱신 (FR-07)
- [x] 성공/실패 토스트(또는 alert) 메시지 표시

---

## M4. 연락처 수정 및 삭제 (Update / Delete)

### 4-1. 연락처 수정 (Update)
> 관련 요구사항: FR-08, FR-09, FR-10, FR-11, US-03

- [x] [수정] 버튼 클릭 시 수정 폼(인라인 또는 모달) 활성화 (FR-08, FR-09)
- [x] 수정 폼에 기존 이름·전화번호 값 미리 채우기
- [x] 수정 폼에서 이름·전화번호 편집 가능하도록 구현
- [x] [저장] 버튼 클릭 시 Supabase `update` 호출
- [x] 수정 완료 후 목록 즉시 갱신 (FR-10)
- [x] [취소] 버튼 클릭 시 수정 폼 닫기 (FR-11)
- [x] 수정 성공/실패 피드백 표시

### 4-2. 연락처 삭제 (Delete)
> 관련 요구사항: FR-12, FR-13, FR-14, US-04

- [x] [삭제] 버튼 구현 (FR-12)
- [x] 삭제 버튼 클릭 시 확인 다이얼로그 표시 (FR-13)
  - 브라우저 기본 `confirm()` 또는 커스텀 모달
- [x] 확인 시 Supabase `delete` 호출 (FR-14)
- [x] 삭제 완료 후 목록 즉시 갱신 (FR-14)
- [x] 삭제 성공/실패 피드백 표시

---

## M5. UI/UX 완성

### 5-1. 기본 레이아웃 및 스타일
- [x] `app/globals.css` 에 전체 디자인 시스템(색상, 폰트, 간격) 정의
- [x] 헤더 컴포넌트 작성 (앱 이름 / 아이콘 표시)
- [x] 입력 폼 영역 스타일 적용
- [x] 연락처 목록 카드/테이블 스타일 적용
- [x] 버튼 공통 스타일 (기본 / 위험 / 취소) 정의

### 5-2. 반응형 (Responsive)
> 관련 요구사항: US-05

- [x] 모바일(360px ~ 767px) 레이아웃 적용
- [x] 태블릿(768px ~ 1279px) 레이아웃 적용
- [x] 데스크톱(1280px+) 레이아웃 적용
- [x] 모바일 Chrome / Safari 에서 직접 테스트

### 5-3. 피드백 UI
- [x] 로딩 스피너 또는 스켈레톤 UI 구현 (데이터 조회 중)
- [x] 토스트 메시지 컴포넌트 구현 (성공 / 실패 / 경고)
- [x] 삭제 확인 모달 커스텀 구현 (선택 — 기본 confirm 대체)

---

## M6. 배포 및 최종 검증

### 6-1. Vercel 배포 설정
- [ ] Vercel 계정 생성 (없는 경우)
- [ ] Vercel 대시보드에서 GitHub 저장소 연결
- [ ] Vercel 환경 변수 설정
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- [ ] 첫 배포 실행 및 빌드 성공 확인

### 6-2. 기능 검증 체크리스트
- [ ] 연락처 목록이 정상 조회되는가?
- [ ] 새 연락처 추가가 정상 동작하는가?
- [ ] 유효성 검사(빈값, 전화번호 형식)가 동작하는가?
- [ ] 연락처 수정이 정상 동작하는가?
- [ ] 수정 취소 시 원래 값이 유지되는가?
- [ ] 연락처 삭제 확인 후 정상 삭제되는가?
- [ ] 목록이 즉시 갱신되는가?
- [ ] 모바일 환경에서 UI가 깨지지 않는가?
- [ ] 페이지 초기 로딩이 3초 이내인가?

### 6-3. 최종 마무리
- [x] `README.md` 작성 (프로젝트 소개, 로컬 실행 방법, 환경 변수 설명)
- [x] 불필요한 `console.log` 제거
- [ ] 프로덕션 빌드 확인 (`npm run build`)
- [ ] 최종 커밋 및 main 브랜치 push

---

## 참고 파일

| 파일 | 설명 |
|------|------|
| [requirement.md](./requirement.md) | 원본 요구사항 |
| [PRD.md](./PRD.md) | 상세 제품 요구사항 문서 |
